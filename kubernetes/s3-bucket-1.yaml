apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  generateName: new-crossplane-bucket-
  labels:
    docs.crossplane.io/example: provider-aws
spec:
  forProvider:
    region: eu-west-1
  providerConfigRef:
    name: default

